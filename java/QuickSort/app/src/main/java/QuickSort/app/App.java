/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package QuickSort.app;

import QuickSort.list.LinkedList;

import static QuickSort.utilities.StringUtils.join;
import static QuickSort.utilities.StringUtils.split;
import static QuickSort.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
